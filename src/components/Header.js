// src/components/Header.js (example header component)
import React from 'react';
import Logout from './Logout';

const Header = () => {
    return (
        <header>
            <Logout />
        </header>
    );
};

export default Header;
